type Register {
    id: ID
    descricao: String
    data_criacao: String
    data_atualizacao: String
    status: String
}

type Content {
    id: ID
    descricao: String
    data_criacao: String
    data_atualizacao: String
    status: String
}

type FindAll {
    content: [Content]
}

type Delete {
    status: Boolean
}